{{ form_start(form) }}
    <div class="container">
        {{ form_widget(form) }}
        <div class="row d-flex justify-content-center">
        <button class="btn btn-outline-success">{{ button_label|default('Enregistrer le nouvel épisode') }}</button>
        </div>
    </div>

{{ form_end(form) }}
